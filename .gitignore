# Rust
/target
Cargo.lock

# Cross-built C libs + build tree (regenerated by third_party/build-gmp-mpfr-wasi.sh)
/vendor

# Built wasm dropped into the web app
/web/src/wasm/*.wasm

# Node / Vite
/web/node_modules
/web/dist

# Playwright verification screenshots (regenerated by web/scripts/verify-browser.mjs)
/web/verify-shots

# Packaged release archives (regenerated by scripts/package-dist.sh)
/dist-artifacts
