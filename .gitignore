# Rust
/target
Cargo.lock

# Cross-built C libs + build tree (regenerated by third_party/build-gmp-mpfr-wasi.sh)
/vendor

# Built wasm dropped into the web app
/web/src/wasm/*.wasm

# Node / Vite
/web/node_modules
/web/dist
